CREATE TABLE Cart
(
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    userid INTEGER REFERENCES users(id),
    itemid INTEGER,
    itemCount INTEGER
   
)

-- CREATE TABLE Cart
-- (
--     id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
--     userid INTEGER REFERENCES users(id),
--     itemid INTEGER 

   
-- )